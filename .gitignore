.DS_Store
*.log

# Supabase credentials — never commit these, see .env.example
.env
.env.local

# Vite / npm
node_modules
dist
dist-ssr
*.local

# Scratch/dev artifacts — screenshots and sample files used while building/testing, not app code
Claude outputs/
_test_invoices/
